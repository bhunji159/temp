import React from "react";
import { StyleSheet, Text, View, Button } from "react-native";

const Login = () => {
  return (
    <View>
      <Button title="login" />
    </View>
  );
};

export default Login;
