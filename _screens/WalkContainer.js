import React from "react";
import { Button, View, Text } from "react-native";

const Walk = ({ navigation }) => {
  return (
    <View>
      <Text> Walk</Text>
    </View>
  );
};

export default Walk;
